#include "main.h"

/**
 * mul - function that multiplies two integers
 * @x: The first number to multiply
 * @y: The second number to multiply with first number
 * Return: result of mul between x and y
 */

int mul(int x, int y)

{
	return (x * y);
}
